import styled from "styled-components";

export const Tag = styled.div`
  padding: 0.3em 0.6em;
  background-color: var(--primary-color);
  color: #fff;
  border-radius: 4px;
  font-size: 0.875em;
`;
